[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "PYTHONPATH=. uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
